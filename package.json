{
  "name": "kiminikimeta",
  "version": "1.0.0",
  "description": "Slack bot chooses a user randomly with slash command.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@at946",
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "^1.5.0"
  }
}
